---
phase: 03-in-calculator-chat-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - components/bioremediation-chat-message.tsx
  - components/bioremediation-chat-feedback.tsx
  - app/api/bioremediation/chat/feedback/route.ts
  - components/bioremediation-chat-panel.tsx
autonomous: true
requirements:
  - CHAT-04
  - CHAT-05
  - AUD-03
must_haves:
  truths:
    - "Assistant answers render as structured recommendation, rationale, confidence, and citations rather than opaque freeform text."
    - "Escalation and low-confidence outcomes clearly route the producer to Aquavet support."
    - "Each assistant answer supports basic usefulness feedback."
  artifacts:
    - path: "components/bioremediation-chat-message.tsx"
      provides: "Structured renderer for answer, clarify, and escalate states"
    - path: "components/bioremediation-chat-feedback.tsx"
      provides: "Per-answer usefulness controls"
    - path: "app/api/bioremediation/chat/feedback/route.ts"
      provides: "Authenticated feedback endpoint tied to assistant messages"
  key_links:
    - from: "components/bioremediation-chat-message.tsx"
      to: "lib/bioremediation-chat/schema.ts"
      via: "Renders the normalized response contract"
      pattern: "recommendation|rationale|confidence|citations"
    - from: "components/bioremediation-chat-feedback.tsx"
      to: "app/api/bioremediation/chat/feedback/route.ts"
      via: "Stores message usefulness"
      pattern: "/api/bioremediation/chat/feedback"
---

<objective>
Finish the in-calculator chat UX by rendering structured answer states and collecting lightweight producer feedback.

Purpose: Make grounded answers legible and safe to act on, especially when the system needs to clarify or escalate.
Output: Structured message renderer, feedback controls, and a minimal feedback persistence route.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-in-calculator-chat-experience/03-CONTEXT.md
@.planning/phases/03-in-calculator-chat-experience/03-RESEARCH.md
@.planning/phases/03-in-calculator-chat-experience/03-01-PLAN.md
@components/bioremediation-chat-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render assistant responses as structured guidance</name>
  <files>components/bioremediation-chat-message.tsx, components/bioremediation-chat-panel.tsx</files>
  <read_first>
    components/bioremediation-chat-panel.tsx
    .planning/phases/02-secure-retrieval-and-audit-backbone/02-02-PLAN.md
    .planning/phases/03-in-calculator-chat-experience/03-CONTEXT.md
  </read_first>
  <action>Create `components/bioremediation-chat-message.tsx` and update the panel to use it for every assistant reply. Render structured sections for recommendation, rationale, confidence, and cited Aquavet cases. Add explicit variants for `clarify` and `escalate`, including a support-oriented next step when no safe grounded answer exists. Keep the calculator guardrail visible in answer states so the numeric dose still points back to the deterministic result.</action>
  <acceptance_criteria>
    `rg -n "Recomendaci[oó]n|Por qu[eé]|Confianza|Casos" components/bioremediation-chat-message.tsx` returns the structured labels.
    `rg -n "clarify|escalate|Aquavet|soporte" components/bioremediation-chat-message.tsx` returns the special states.
    `rg -n "calculadora|fuente de verdad|dosis" components/bioremediation-chat-message.tsx` returns the numeric guardrail copy.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>Assistant replies become scannable, bounded, and safer for producers to interpret.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-answer usefulness feedback</name>
  <files>components/bioremediation-chat-feedback.tsx, app/api/bioremediation/chat/feedback/route.ts</files>
  <read_first>
    .planning/phases/02-secure-retrieval-and-audit-backbone/02-01-PLAN.md
    .planning/phases/03-in-calculator-chat-experience/03-RESEARCH.md
  </read_first>
  <action>Create a minimal feedback flow for assistant answers. Add `components/bioremediation-chat-feedback.tsx` with simple useful / not useful controls and wire it to an authenticated `app/api/bioremediation/chat/feedback/route.ts`. Persist feedback against the assistant message record created in Phase 2. Keep the interaction lightweight and resilient to repeat clicks or stale sessions.</action>
  <acceptance_criteria>
    `rg -n "útil|no útil|useful|not useful" components/bioremediation-chat-feedback.tsx` returns feedback labels.
    `rg -n "export async function POST" app/api/bioremediation/chat/feedback/route.ts` returns the feedback route.
    `rg -n "messageId|feedback" components/bioremediation-chat-feedback.tsx app/api/bioremediation/chat/feedback/route.ts` returns the persistence contract.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The product can capture whether an assistant answer helped the producer.</done>
</task>

</tasks>

<verification>
Run `pnpm lint && pnpm build`. In `pnpm dev`, manually validate three paths: a grounded answer with citations, a clarifying follow-up when context is incomplete, and an escalation response with visible Aquavet support guidance. Confirm feedback submits only for assistant messages.
</verification>

<success_criteria>
Phase 3 finishes the chat experience with structured safe answers, explicit escalation behavior, and a minimal producer feedback loop.
</success_criteria>

<output>
After completion, create `.planning/phases/03-in-calculator-chat-experience/03-02-SUMMARY.md`
</output>
