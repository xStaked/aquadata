---
phase: 03-in-calculator-chat-experience
plan: 01
type: execute
wave: 2
depends_on:
  - ../02-secure-retrieval-and-audit-backbone/02-02-PLAN.md
files_modified:
  - components/bioremediation-chat-panel.tsx
  - hooks/use-bioremediation-chat.ts
  - components/bioremediation-form.tsx
  - app/dashboard/bioremediation/page.tsx
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
must_haves:
  truths:
    - "A producer can access the assistant without leaving the current calculator route."
    - "Every sent chat message automatically includes the active calculator context when available."
    - "Clarifying questions are surfaced as a first-class part of the experience when context is missing."
  artifacts:
    - path: "hooks/use-bioremediation-chat.ts"
      provides: "Client-side session, send-state, and request orchestration for the calculator assistant"
    - path: "components/bioremediation-chat-panel.tsx"
      provides: "Embedded calculator assistant shell"
    - path: "components/bioremediation-form.tsx"
      provides: "Integration point that keeps calculator and assistant in one workflow"
  key_links:
    - from: "hooks/use-bioremediation-chat.ts"
      to: "app/api/bioremediation/chat/route.ts"
      via: "Calls the Phase 2 protected chat route"
      pattern: "/api/bioremediation/chat"
    - from: "components/bioremediation-form.tsx"
      to: "hooks/use-bioremediation.ts"
      via: "Shares active calculator context"
      pattern: "result|selectedProduct|species|aeration"
---

<objective>
Embed the assistant into the existing calculator workflow without displacing the current deterministic experience.

Purpose: Deliver the client-side hook and panel shell that let producers ask grounded dosage questions in-context.
Output: One embedded assistant panel, one chat state hook, and calculator integration that passes current context into the Phase 2 backend.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-in-calculator-chat-experience/03-CONTEXT.md
@.planning/phases/03-in-calculator-chat-experience/03-RESEARCH.md
@.planning/phases/02-secure-retrieval-and-audit-backbone/02-02-PLAN.md
@app/dashboard/bioremediation/page.tsx
@components/bioremediation-form.tsx
@hooks/use-bioremediation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a dedicated calculator-chat state hook</name>
  <files>hooks/use-bioremediation-chat.ts</files>
  <read_first>
    hooks/use-bioremediation.ts
    .planning/phases/02-secure-retrieval-and-audit-backbone/02-02-PLAN.md
    .planning/phases/03-in-calculator-chat-experience/03-CONTEXT.md
  </read_first>
  <action>Create `hooks/use-bioremediation-chat.ts` to own the embedded assistant state: draft message, messages, send lifecycle, session ID, and serialization of current calculator context. The hook should accept the active calculator state from `useBioremediation()` and call `/api/bioremediation/chat` with that context on send. Treat the normalized backend response kinds `answer`, `clarify`, and `escalate` as first-class UI states rather than raw text.</action>
  <acceptance_criteria>
    `rg -n "/api/bioremediation/chat" hooks/use-bioremediation-chat.ts` returns a match.
    `rg -n "sessionId|messages|isSending|sendMessage|calculatorContext" hooks/use-bioremediation-chat.ts` returns the expected hook state.
    `rg -n "answer|clarify|escalate" hooks/use-bioremediation-chat.ts` returns the handled response kinds.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The chat behavior is isolated in one client hook and ready to be mounted in the calculator UI.</done>
</task>

<task type="auto">
  <name>Task 2: Create the embedded assistant panel</name>
  <files>components/bioremediation-chat-panel.tsx</files>
  <read_first>
    components/bioremediation-form.tsx
    components/ui/card.tsx
    components/ui/input.tsx
    components/ui/button.tsx
  </read_first>
  <action>Create `components/bioremediation-chat-panel.tsx` as the bounded assistant shell rendered inside the calculator workflow. Include a short scope note, message area, composer, loading/empty states, and a compact summary of current calculator context when available. The panel should visually reinforce that the calculator remains primary and that the assistant is for dosage clarification and grounded troubleshooting only.</action>
  <acceptance_criteria>
    `rg -n "bioremediacion|calculadora|contexto|Aquavet" components/bioremediation-chat-panel.tsx` returns Spanish-first scope copy.
    `rg -n "messages|textarea|input|send|loading|empty" components/bioremediation-chat-panel.tsx` returns the expected panel states.
    `rg -n "dosis|especie|producto|aireacion" components/bioremediation-chat-panel.tsx` returns the current-context summary fields.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The calculator has an embedded assistant surface that is clearly contextual rather than standalone.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate the panel into the existing calculator workflow</name>
  <files>components/bioremediation-form.tsx, app/dashboard/bioremediation/page.tsx</files>
  <read_first>
    components/bioremediation-form.tsx
    hooks/use-bioremediation.ts
    hooks/use-bioremediation-chat.ts
    components/bioremediation-chat-panel.tsx
  </read_first>
  <action>Update the current calculator route and form so the assistant panel is mounted in a natural location without displacing the existing form and result card. Feed the active calculator context and deterministic result into the chat hook and panel. Keep the calculator CTA and save flow intact, and ensure the user can ask a question before or after calculating, with richer context once a result exists.</action>
  <acceptance_criteria>
    `rg -n "BioremediationChatPanel" components/bioremediation-form.tsx app/dashboard/bioremediation/page.tsx` returns the mounted panel.
    `rg -n "useBioremediationChat|calculatorContext|result" components/bioremediation-form.tsx` returns the context handoff.
    `rg -n "Calcular dosis|Guardar cálculo" components/bioremediation-form.tsx` still returns the original calculator controls after integration.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The assistant is reachable inside the calculator without breaking the existing deterministic flow.</done>
</task>

</tasks>

<verification>
Run `pnpm lint && pnpm build`. In `pnpm dev`, confirm the assistant appears on `/dashboard/bioremediation`, inherits current calculator context, and shows a clarifying question instead of fabricating an answer when required fields are missing.
</verification>

<success_criteria>
Phase 3 exposes the assistant where producers already calculate dosage and automatically carries the current calculator context into each question.
</success_criteria>

<output>
After completion, create `.planning/phases/03-in-calculator-chat-experience/03-01-SUMMARY.md`
</output>
