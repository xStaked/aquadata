---
phase: 02-secure-retrieval-and-audit-backbone
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01-PLAN.md
files_modified:
  - lib/ai/provider.ts
  - lib/bioremediation-chat/deepseek.ts
  - lib/bioremediation-chat/service.ts
  - app/api/bioremediation/chat/route.ts
autonomous: true
requirements:
  - SAFE-01
  - SAFE-04
  - SAFE-05
  - AUD-01
  - AUD-02
  - CHAT-06
must_haves:
  truths:
    - "Producer chat requests are processed through an authenticated server-side route."
    - "DeepSeek invocation is isolated behind a repo-local adapter rather than spread across handlers."
    - "Each stored assistant answer includes citations, confidence, and escalation state while preserving calculator authority."
  artifacts:
    - path: "lib/bioremediation-chat/deepseek.ts"
      provides: "Server-only DeepSeek adapter"
    - path: "lib/bioremediation-chat/service.ts"
      provides: "Answer orchestration that combines retrieval, prompting, storage, and guardrails"
    - path: "app/api/bioremediation/chat/route.ts"
      provides: "Authenticated producer chat endpoint for the calculator assistant"
  key_links:
    - from: "app/api/bioremediation/chat/route.ts"
      to: "lib/bioremediation-chat/service.ts"
      via: "Single orchestration entrypoint"
      pattern: "createBioremediationChatResponse|handleBioremediationChat"
    - from: "lib/bioremediation-chat/service.ts"
      to: "lib/bioremediation-chat/retrieval.ts"
      via: "Retrieval before generation"
      pattern: "retrieve.*Case|rank.*Case"
    - from: "lib/bioremediation-chat/service.ts"
      to: "hooks/use-bioremediation.ts"
      via: "Calculator guardrail semantics"
      pattern: "finalDoseG|calculator"
---

<objective>
Implement the authenticated server-side chat pipeline so the calculator assistant can call DeepSeek safely with approved evidence and stored audit traces.

Purpose: Deliver the provider adapter, orchestration layer, and route contract needed for later UI integration.
Output: Server-only DeepSeek adapter, grounded answer service, and protected bioremediation chat route.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-secure-retrieval-and-audit-backbone/02-CONTEXT.md
@.planning/phases/02-secure-retrieval-and-audit-backbone/02-RESEARCH.md
@.planning/phases/02-secure-retrieval-and-audit-backbone/02-01-PLAN.md
@app/api/ocr/route.ts
@lib/ai/provider.ts
@lib/supabase/server.ts
@hooks/use-bioremediation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a server-only DeepSeek adapter</name>
  <files>lib/ai/provider.ts, lib/bioremediation-chat/deepseek.ts</files>
  <read_first>
    lib/ai/provider.ts
    app/api/ocr/route.ts
    .planning/phases/02-secure-retrieval-and-audit-backbone/02-CONTEXT.md
  </read_first>
  <action>Extend the current provider boundary so text generation for the calculator assistant can use DeepSeek without leaking provider details into route handlers. Create `lib/bioremediation-chat/deepseek.ts` as a server-only adapter that accepts normalized prompt input and returns a typed structured result. If `lib/ai/provider.ts` is updated, keep the change minimal and compatible with the rest of the repo's AI usage.</action>
  <acceptance_criteria>
    `rg -n "deepseek" lib/ai/provider.ts lib/bioremediation-chat/deepseek.ts` returns the new provider boundary.
    `rg -n "server-only|process\\.env|api key|authorization" lib/bioremediation-chat/deepseek.ts` shows the adapter stays server-side and env-driven.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The codebase has exactly one DeepSeek integration seam for the bioremediation assistant.</done>
</task>

<task type="auto">
  <name>Task 2: Build the grounded answer orchestration service</name>
  <files>lib/bioremediation-chat/service.ts</files>
  <read_first>
    lib/bioremediation-chat/schema.ts
    lib/bioremediation-chat/types.ts
    lib/bioremediation-chat/retrieval.ts
    hooks/use-bioremediation.ts
  </read_first>
  <action>Create `lib/bioremediation-chat/service.ts` to orchestrate request parsing, retrieval, prompt assembly, DeepSeek invocation, response normalization, and audit persistence. The service must carry through calculator context, distinguish `answer` vs `clarify` vs `escalate`, persist cited case IDs and confidence, and include a calculator guardrail note so the numeric dose remains visibly deterministic. If retrieval returns no safe evidence, the service should decline or escalate instead of improvising unsupported guidance.</action>
  <acceptance_criteria>
    `rg -n "answer|clarify|escalate" lib/bioremediation-chat/service.ts` returns the response kinds.
    `rg -n "cited_case_ids|confidence|requires_escalation|low_confidence" lib/bioremediation-chat/service.ts` returns audit persistence fields.
    `rg -n "calculator|deterministic|source of truth|dose" lib/bioremediation-chat/service.ts` returns the guardrail logic.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The backend can produce one bounded structured answer contract from calculator context plus approved case evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Expose the authenticated producer chat route</name>
  <files>app/api/bioremediation/chat/route.ts</files>
  <read_first>
    lib/supabase/server.ts
    app/api/ocr/route.ts
    lib/bioremediation-chat/service.ts
    .planning/phases/02-secure-retrieval-and-audit-backbone/02-CONTEXT.md
  </read_first>
  <action>Create `app/api/bioremediation/chat/route.ts` as the single backend entry point for calculator chat. Validate the request with the shared schemas, require an authenticated user, attach organization-safe context, call the orchestration service, and return only the normalized structured response. Do not expose the DeepSeek provider or raw prompts to the browser. Log or return clear errors for unauthorized access and invalid payloads.</action>
  <acceptance_criteria>
    `rg -n "export async function POST" app/api/bioremediation/chat/route.ts` returns a match.
    `rg -n "createServerClient|supabase\\.auth|getUser|require" app/api/bioremediation/chat/route.ts` returns authenticated server-side user handling.
    `rg -n "lib/bioremediation-chat/service" app/api/bioremediation/chat/route.ts` returns the orchestration import.
    `rg -n "question|calculatorContext|sessionId" app/api/bioremediation/chat/route.ts` returns the bounded route input shape.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The project exposes a protected route for grounded bioremediation chat without any browser-side model access.</done>
</task>

</tasks>

<verification>
Run `pnpm lint && pnpm build`. In `pnpm dev`, manually confirm that unauthenticated requests fail, safe requests return a normalized answer shape, and unsupported contexts produce clarify or escalate outcomes rather than fabricated dosage guidance.
</verification>

<success_criteria>
Phase 2 is executable end-to-end: the server can accept a producer chat request, retrieve only approved case evidence, invoke DeepSeek safely, store the trace, and keep the calculator as the authoritative numeric source.
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-retrieval-and-audit-backbone/02-02-SUMMARY.md`
</output>
