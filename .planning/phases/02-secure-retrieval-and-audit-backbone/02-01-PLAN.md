---
phase: 02-secure-retrieval-and-audit-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/021_bioremediation_chat.sql
  - lib/bioremediation-chat/schema.ts
  - lib/bioremediation-chat/types.ts
  - lib/bioremediation-chat/retrieval.ts
autonomous: true
requirements:
  - SAFE-02
  - SAFE-03
  - SAFE-04
  - SAFE-05
  - AUD-01
  - AUD-02
must_haves:
  truths:
    - "Approved case-library rows are the only records eligible for retrieval and citation."
    - "Chat sessions and messages persist enough structured context to support audit and tenant isolation."
    - "The retrieval layer filters by calculator-aware structured context before any model call happens."
  artifacts:
    - path: "scripts/021_bioremediation_chat.sql"
      provides: "Session/message persistence plus tenant-safe RLS for bioremediation chat"
    - path: "lib/bioremediation-chat/schema.ts"
      provides: "Shared Zod contracts for calculator context, messages, citations, and response kinds"
    - path: "lib/bioremediation-chat/types.ts"
      provides: "Typed session, message, and retrieval candidate interfaces"
    - path: "lib/bioremediation-chat/retrieval.ts"
      provides: "Approved-case retrieval and deterministic ranking helper"
  key_links:
    - from: "lib/bioremediation-chat/retrieval.ts"
      to: "scripts/020_case_library.sql"
      via: "Reads from governed approved cases only"
      pattern: "from\\('bioremediation_cases'\\)|status_usable_for_grounding"
    - from: "lib/bioremediation-chat/schema.ts"
      to: "hooks/use-bioremediation.ts"
      via: "Reuses calculator context field semantics"
      pattern: "species|aeration|stockingDensity|finalDoseG"
---

<objective>
Create the persistence and retrieval contract for the bioremediation assistant so later route and UI work can rely on one tenant-safe, case-grounded backend.

Purpose: Establish audit-ready chat tables, shared schemas/types, and a deterministic retrieval helper that reads only approved case-library evidence.
Output: SQL migration, shared contracts, and retrieval module for the future authenticated chat route.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-secure-retrieval-and-audit-backbone/02-CONTEXT.md
@.planning/phases/02-secure-retrieval-and-audit-backbone/02-RESEARCH.md
@.planning/phases/01-case-library-governance/01-CONTEXT.md
@hooks/use-bioremediation.ts
@lib/case-library/schema.ts
@lib/case-library/types.ts
@scripts/020_case_library.sql
@lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the audit-ready chat persistence model</name>
  <files>scripts/021_bioremediation_chat.sql</files>
  <read_first>
    scripts/001_create_schema.sql
    scripts/010_admin_module.sql
    scripts/020_case_library.sql
    .planning/phases/02-secure-retrieval-and-audit-backbone/02-CONTEXT.md
  </read_first>
  <action>Create `scripts/021_bioremediation_chat.sql` with tables for `bioremediation_chat_sessions` and `bioremediation_chat_messages`. Store `user_id`, `organization_id`, a JSON calculator-context snapshot, and optional last deterministic dose on the session row. Store `role`, `content`, `response_kind`, `confidence`, `cited_case_ids`, `calculator_context`, `low_confidence`, and `requires_escalation` on message rows. Add timestamps, foreign keys, and RLS policies that allow authenticated producers to read and write only their own organization-scoped sessions and messages. Do not create cross-tenant shared transcript access or any vector-search tables in this phase.</action>
  <acceptance_criteria>
    `rg -n "CREATE TABLE IF NOT EXISTS public\\.bioremediation_chat_sessions" scripts/021_bioremediation_chat.sql` returns a match.
    `rg -n "CREATE TABLE IF NOT EXISTS public\\.bioremediation_chat_messages" scripts/021_bioremediation_chat.sql` returns a match.
    `rg -n "organization_id|calculator_context|cited_case_ids|requires_escalation|low_confidence" scripts/021_bioremediation_chat.sql` returns all required audit fields.
    `rg -n "ENABLE ROW LEVEL SECURITY|CREATE POLICY" scripts/021_bioremediation_chat.sql` returns the expected RLS blocks.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>Chat persistence exists with explicit tenant scoping and enough audit structure for later answer review.</done>
</task>

<task type="auto">
  <name>Task 2: Define shared calculator-chat contracts</name>
  <files>lib/bioremediation-chat/schema.ts, lib/bioremediation-chat/types.ts</files>
  <read_first>
    hooks/use-bioremediation.ts
    lib/case-library/types.ts
    .planning/phases/02-secure-retrieval-and-audit-backbone/02-RESEARCH.md
  </read_first>
  <action>Create `lib/bioremediation-chat/schema.ts` and `lib/bioremediation-chat/types.ts`. Model one normalized calculator context schema containing the current product, species, area, depth, age, stocking density, aeration, and optional deterministic dose result. Export response-kind enums for `answer`, `clarify`, and `escalate`, plus citation and message schemas. Keep field names aligned with `hooks/use-bioremediation.ts` so Phase 3 can serialize current calculator state without lossy mapping.</action>
  <acceptance_criteria>
    `rg -n "calculatorContextSchema|responseKindValues|citationSchema" lib/bioremediation-chat/schema.ts` returns exported shared contracts.
    `rg -n "BioremediationChat(Session|Message|Citation|CalculatorContext)" lib/bioremediation-chat/types.ts` returns exported types.
    `rg -n "species|product|areaM2|depth|ageMonths|stockingDensity|aeration|finalDoseG" lib/bioremediation-chat/schema.ts` returns the key calculator context fields.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The backend and later UI share one stable contract for calculator context, messages, and citations.</done>
</task>

<task type="auto">
  <name>Task 3: Implement approved-case retrieval and deterministic ranking</name>
  <files>lib/bioremediation-chat/retrieval.ts</files>
  <read_first>
    lib/case-library/types.ts
    scripts/020_case_library.sql
    hooks/use-bioremediation.ts
    .planning/phases/02-secure-retrieval-and-audit-backbone/02-CONTEXT.md
  </read_first>
  <action>Create `lib/bioremediation-chat/retrieval.ts` as the pre-generation evidence layer. Query only approved and grounding-eligible `bioremediation_cases`, apply structured filters from calculator context and question metadata, and return a small ranked set of candidates plus exclusion reasons when nothing is safe enough to cite. Keep ranking pragmatic and transparent: exact product/species matches should score above looser matches, and the helper should surface low-evidence situations clearly for later clarify/escalate behavior.</action>
  <acceptance_criteria>
    `rg -n "status.*approved|status_usable_for_grounding" lib/bioremediation-chat/retrieval.ts` returns the approved-case guard.
    `rg -n "product_name|species|zone" lib/bioremediation-chat/retrieval.ts` returns the structured filtering dimensions.
    `rg -n "rank|score|lowEvidence|insufficient" lib/bioremediation-chat/retrieval.ts` returns deterministic ranking or fallback signals.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The project has a deterministic retrieval layer that enforces case-governance rules before any model invocation.</done>
</task>

</tasks>

<verification>
Run `pnpm lint && pnpm build`. In `pnpm dev`, verify the retrieval helper can only target approved case-library rows and that no chat data shape implies cross-tenant sharing.
</verification>

<success_criteria>
Phase 2 has the tenant-safe storage and retrieval backbone required for grounded bioremediation chat: approved-case filtering is enforced, audit fields are explicit, and calculator context has one stable contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-retrieval-and-audit-backbone/02-01-SUMMARY.md`
</output>
