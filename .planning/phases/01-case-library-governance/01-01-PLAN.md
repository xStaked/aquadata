---
phase: 01-case-library-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/020_case_library.sql
  - lib/case-library/schema.ts
  - lib/case-library/types.ts
  - app/admin/bioremediation/cases/actions.ts
autonomous: true
requirements:
  - CASE-01
  - CASE-03
  - CASE-04
must_haves:
  truths:
    - "Admin-authenticated writes can persist a structured bioremediation case without direct database access."
    - "Each case row stores the latest governance metadata required for review and publication state."
    - "Status transitions are constrained to draft, approved, and retired, with approved as the only grounding-eligible state."
  artifacts:
    - path: "scripts/020_case_library.sql"
      provides: "Governed bioremediation_cases table, indexes, and admin-compatible RLS policies"
    - path: "lib/case-library/schema.ts"
      provides: "Shared Zod schema and status constants for create/update actions"
    - path: "lib/case-library/types.ts"
      provides: "Typed row and form contracts for the case-library feature"
    - path: "app/admin/bioremediation/cases/actions.ts"
      provides: "Admin-only server actions for create, edit, and status changes"
  key_links:
    - from: "app/admin/bioremediation/cases/actions.ts"
      to: "lib/case-library/schema.ts"
      via: "Zod parsing before writes"
      pattern: "caseInputSchema\\.parse|safeParse"
    - from: "app/admin/bioremediation/cases/actions.ts"
      to: "lib/auth/roles.ts"
      via: "Admin gate"
      pattern: "requireAdminUser\\("
    - from: "app/admin/bioremediation/cases/actions.ts"
      to: "scripts/020_case_library.sql"
      via: "Writes to public.bioremediation_cases"
      pattern: "from\\('bioremediation_cases'\\)"
---

<objective>
Create the governed persistence and write contract for the Phase 1 case library so later UI work can build against a stable admin-only CRUD surface.

Purpose: Establish the structured case model, latest-state review metadata, and admin-only mutation path mandated by D-01 through D-04.
Output: SQL migration, shared case-library schema/types, and server actions for create/update/status transitions that the Phase 1 admin surface will invoke directly.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-case-library-governance/01-CONTEXT.md
@.planning/phases/01-case-library-governance/01-RESEARCH.md
@.planning/phases/01-case-library-governance/01-VALIDATION.md
@app/admin/bioremediation/page.tsx
@lib/auth/roles.ts
@scripts/001_create_schema.sql
@scripts/010_admin_module.sql

<interfaces>
From `lib/auth/roles.ts`:
```ts
export const ADMIN_ROLES = ['admin'] as const
export function isAdminRole(role: string | null | undefined): boolean
export async function requireAdminUser(): Promise<{ supabase: unknown; user: { id: string }; profile: { id: string; role: string; full_name: string | null } | null }>
```

Locked decisions from `01-CONTEXT.md`:
- D-01: Any existing `admin` can move a case between `draft`, `approved`, and `retired`
- D-02: Do not add approver-only logic or author-vs-reviewer separation
- D-03: Store only latest reviewer metadata on the main case row
- D-04: Do not add transition-history tables in Phase 1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the shared case-library contracts</name>
  <files>lib/case-library/schema.ts, lib/case-library/types.ts</files>
  <read_first>
    lib/auth/roles.ts
    app/admin/bioremediation/page.tsx
    .planning/phases/01-case-library-governance/01-CONTEXT.md
    .planning/phases/01-case-library-governance/01-RESEARCH.md
  </read_first>
  <action>Create `lib/case-library/schema.ts` and `lib/case-library/types.ts` as the interface-first contract for Phase 1. In `schema.ts`, export `caseStatusValues = ['draft', 'approved', 'retired'] as const`, a `caseInputSchema` with required fields `issue`, `zone`, `species`, `product_name`, `treatment_approach`, `dose`, `dose_unit`, `outcome`, and `status`, plus optional `id` and `notes`. Use `z.coerce.number().positive()` for `dose`, default `dose_unit` to `'L'`, and keep field names aligned with the future SQL columns. In `types.ts`, export row/form types such as `BioremediationCaseStatus`, `BioremediationCaseFormValues`, and `BioremediationCaseRow`, including latest-state metadata fields `author_id`, `last_reviewed_by`, `last_reviewed_at`, and `status_usable_for_grounding`. Do not add retrieval, chat, or history types.</action>
  <acceptance_criteria>
    `rg -n "export const caseStatusValues = \\['draft', 'approved', 'retired'\\] as const" lib/case-library/schema.ts` returns a match.
    `rg -n "product_name|treatment_approach|dose_unit|status" lib/case-library/schema.ts` returns all required structured field names.
    `rg -n "export type BioremediationCase(Status|FormValues|Row)" lib/case-library/types.ts` returns exported feature contracts.
    `rg -n "last_reviewed_by|last_reviewed_at|status_usable_for_grounding" lib/case-library/types.ts` returns metadata and grounding-eligibility fields.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The feature has a single shared schema and type contract that downstream UI and actions can import directly without inventing field names or status values.</done>
</task>

<task type="auto">
  <name>Task 2: Add the governed case-library migration and policies</name>
  <files>scripts/020_case_library.sql</files>
  <read_first>
    scripts/001_create_schema.sql
    scripts/010_admin_module.sql
    .planning/phases/01-case-library-governance/01-CONTEXT.md
    .planning/phases/01-case-library-governance/01-RESEARCH.md
  </read_first>
  <action>Create `scripts/020_case_library.sql` with a new `public.bioremediation_cases` table. Include columns `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `issue TEXT NOT NULL`, `zone TEXT NOT NULL`, `species TEXT NOT NULL`, `product_name TEXT NOT NULL`, `treatment_approach TEXT NOT NULL`, `dose NUMERIC NOT NULL`, `dose_unit TEXT NOT NULL DEFAULT 'L'`, `outcome TEXT NOT NULL`, `status TEXT NOT NULL CHECK (status IN ('draft', 'approved', 'retired'))`, `status_usable_for_grounding BOOLEAN NOT NULL DEFAULT false`, `author_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE RESTRICT`, `last_reviewed_by UUID REFERENCES auth.users(id) ON DELETE SET NULL`, `last_reviewed_at TIMESTAMPTZ`, `notes TEXT`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, and `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`. Add `ENABLE ROW LEVEL SECURITY`, admin full-access policy using `public.is_admin(auth.uid())`, and a check or trigger-level rule that keeps `status_usable_for_grounding = true` only when `status = 'approved'`. Follow D-03 and D-04 exactly: latest-state metadata only, no history table, no approver-only role logic.</action>
  <acceptance_criteria>
    `rg -n "CREATE TABLE IF NOT EXISTS public\\.bioremediation_cases" scripts/020_case_library.sql` returns a match.
    `rg -n "CHECK \\(status IN \\('draft', 'approved', 'retired'\\)\\)" scripts/020_case_library.sql` returns a match.
    `rg -n "status_usable_for_grounding BOOLEAN NOT NULL DEFAULT false" scripts/020_case_library.sql` returns a match.
    `rg -n "author_id UUID NOT NULL REFERENCES auth\\.users\\(id\\)|last_reviewed_by UUID REFERENCES auth\\.users\\(id\\)|last_reviewed_at TIMESTAMPTZ" scripts/020_case_library.sql` returns all governance metadata columns.
    `rg -n "CREATE POLICY \\\"admin_full_access_bioremediation_cases\\\"" scripts/020_case_library.sql` returns a match.
    `rg -n "history|audit" scripts/020_case_library.sql` returns no matches for new tables or policies beyond comments.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The database has a governed case-library table and admin-compatible RLS that can store structured cases and latest-state review metadata without expanding into later-phase audit scope.</done>
</task>

<task type="auto">
  <name>Task 3: Implement admin-only server actions for create, edit, and publication state changes</name>
  <files>app/admin/bioremediation/cases/actions.ts</files>
  <read_first>
    app/admin/actions.ts
    lib/auth/roles.ts
    lib/case-library/schema.ts
    lib/case-library/types.ts
    .planning/phases/01-case-library-governance/01-CONTEXT.md
  </read_first>
  <action>Create `app/admin/bioremediation/cases/actions.ts` with `'use server'` actions `upsertBioremediationCase` and `transitionBioremediationCaseStatus`. Both actions must call `requireAdminUser()` first, parse inputs from the shared Zod contract, and write through the server Supabase client only. For inserts, set `author_id = user.id`. For transitions to `approved` or `retired`, set `last_reviewed_by = user.id` and `last_reviewed_at = new Date().toISOString()` per D-01 and D-03; for `draft`, clear `status_usable_for_grounding` and keep review metadata behavior explicit rather than implicit. Revalidate `/admin/bioremediation` and `/admin/bioremediation/cases`. Shape the action signatures so Plan 02 can call them directly from mounted create/edit dialogs and row-level status controls in `app/admin/bioremediation/cases/page.tsx` and `components/admin/case-library/case-table.tsx`. Do not add API routes, browser-side Supabase writes, or any role checks beyond the existing `admin` role.</action>
  <acceptance_criteria>
    `rg -n "^'use server'" app/admin/bioremediation/cases/actions.ts` returns a match.
    `rg -n "export async function (upsertBioremediationCase|transitionBioremediationCaseStatus)" app/admin/bioremediation/cases/actions.ts` returns both action names.
    `rg -n "requireAdminUser\\(\\)" app/admin/bioremediation/cases/actions.ts` returns at least one match.
    `rg -n "caseInputSchema\\.(parse|safeParse)" app/admin/bioremediation/cases/actions.ts` returns a match.
    `rg -n "from\\('bioremediation_cases'\\)" app/admin/bioremediation/cases/actions.ts` returns write operations on the governed table.
    `rg -n "revalidatePath\\('/admin/bioremediation'\\)|revalidatePath\\('/admin/bioremediation/cases'\\)" app/admin/bioremediation/cases/actions.ts` returns both invalidation targets.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The admin surface has a single authenticated mutation layer for create, edit, and status changes that matches the governed schema and is ready for direct invocation from the Phase 1 page-level create/edit entrypoints and table status controls.</done>
</task>

</tasks>

<verification>
Run `pnpm lint && pnpm build` after this plan. In `pnpm dev`, confirm the new actions are available to the next plan without introducing browser-side Supabase writes or unauthorized admin bypasses.
</verification>

<success_criteria>
Phase 1 has a stable persistence and write contract for governed cases: structured required fields exist, the only supported statuses are `draft`, `approved`, and `retired`, latest-state review metadata is modeled on the row, and admin-only server actions are ready for the management UI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-case-library-governance/01-01-SUMMARY.md`
</output>
