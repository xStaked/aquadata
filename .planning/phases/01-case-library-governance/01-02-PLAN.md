---
phase: 01-case-library-governance
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/admin/bioremediation/page.tsx
  - app/admin/bioremediation/cases/page.tsx
  - components/admin/case-library/case-form-dialog.tsx
  - components/admin/case-library/case-status-badge.tsx
  - components/admin/case-library/case-table.tsx
  - .planning/phases/01-case-library-governance/01-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - CASE-02
  - OPS-01
must_haves:
  truths:
    - "Aquavet admin can open the case library from the existing admin surface."
    - "Aquavet admin can create and edit a case from the product UI and see the latest saved values after returning to the list."
    - "Aquavet admin can review author, last review date, and publication state while managing cases."
  artifacts:
    - path: "app/admin/bioremediation/cases/page.tsx"
      provides: "Server-rendered case-library index with filters and management entrypoints"
    - path: "components/admin/case-library/case-form-dialog.tsx"
      provides: "Create/edit dialog for structured case fields"
    - path: "components/admin/case-library/case-table.tsx"
      provides: "Management table with governance metadata and status controls"
    - path: "components/admin/case-library/case-status-badge.tsx"
      provides: "Consistent draft/approved/retired visual state"
    - path: ".planning/phases/01-case-library-governance/01-MANUAL-CHECKLIST.md"
      provides: "Manual smoke checks for CRUD, status transitions, and admin-shell access"
  key_links:
    - from: "app/admin/bioremediation/cases/page.tsx"
      to: "app/admin/bioremediation/cases/actions.ts"
      via: "Dialog and row actions"
      pattern: "upsertBioremediationCase|transitionBioremediationCaseStatus"
    - from: "app/admin/bioremediation/page.tsx"
      to: "app/admin/bioremediation/cases/page.tsx"
      via: "CTA or module entrypoint"
      pattern: "/admin/bioremediation/cases"
    - from: "components/admin/case-library/case-table.tsx"
      to: "components/admin/case-library/case-status-badge.tsx"
      via: "Rendered publication state"
      pattern: "CaseStatusBadge"
---

<objective>
Deliver the admin case-library management surface inside the existing `/admin` experience so Aquavet can review, create, edit, and govern case records without database access.

Purpose: Satisfy the product-facing Phase 1 workflows on top of the write contract from Plan 01.
Output: Case-library route, supporting admin components, integration from the bioremediation admin module, and a manual verification checklist.
</objective>

<execution_context>
@/Users/xstaked/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xstaked/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-case-library-governance/01-CONTEXT.md
@.planning/phases/01-case-library-governance/01-RESEARCH.md
@.planning/phases/01-case-library-governance/01-VALIDATION.md
@.planning/phases/01-case-library-governance/01-01-PLAN.md
@app/admin/layout.tsx
@app/admin/bioremediation/page.tsx
@app/admin/producers/page.tsx
@app/admin/bioremediation/cases/actions.ts
@lib/case-library/schema.ts
@lib/case-library/types.ts

<interfaces>
From `app/admin/bioremediation/cases/actions.ts`:
```ts
export async function upsertBioremediationCase(input: unknown): Promise<void>
export async function transitionBioremediationCaseStatus(input: { id: string; status: 'draft' | 'approved' | 'retired' }): Promise<void>
```

UI constraints to preserve:
- Spanish-first copy
- server-rendered admin page with lightweight client dialogs
- no new approver-only workflow, no status history UI, no retrieval/chat controls
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the server-rendered case-library index and management table</name>
  <files>app/admin/bioremediation/cases/page.tsx, components/admin/case-library/case-status-badge.tsx, components/admin/case-library/case-table.tsx</files>
  <read_first>
    app/admin/layout.tsx
    app/admin/producers/page.tsx
    app/admin/bioremediation/page.tsx
    app/admin/bioremediation/cases/actions.ts
    lib/case-library/types.ts
    .planning/phases/01-case-library-governance/01-CONTEXT.md
  </read_first>
  <action>Create `app/admin/bioremediation/cases/page.tsx` as an async server component protected with `requireAdminUser()`. Read `searchParams` for at least `status`, `species`, and `product`, query `bioremediation_cases`, and render KPI cards plus a table-first management view consistent with `app/admin/producers/page.tsx`. Add `components/admin/case-library/case-status-badge.tsx` to render `draft`, `approved`, and `retired` consistently, and `components/admin/case-library/case-table.tsx` to display columns for `issue`, `zone`, `species`, `product_name`, `status`, `author`, `last_reviewed_at`, and a grounding-eligibility indicator tied to approved status. Keep the page server-rendered and lightweight; do not introduce client-side data fetching or a detached `/admin/cases` module.</action>
  <acceptance_criteria>
    `rg -n "export default async function .*Case.*Page|requireAdminUser\\(\\)" app/admin/bioremediation/cases/page.tsx` returns both the async page and admin gate.
    `rg -n "searchParams: Promise<\\{ status\\?: string; species\\?: string; product\\?: string \\}>" app/admin/bioremediation/cases/page.tsx` returns the expected filter contract.
    `rg -n "from\\('bioremediation_cases'\\)" app/admin/bioremediation/cases/page.tsx` returns the list query.
    `rg -n "issue|zone|species|product_name|last_reviewed_at" components/admin/case-library/case-table.tsx` returns the governance table columns.
    `rg -n "CaseStatusBadge" components/admin/case-library/case-table.tsx components/admin/case-library/case-status-badge.tsx` returns usage and export matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>The case library has a server-rendered admin index with filters, governance metadata, and publication-state visibility aligned with the repo’s current admin module patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Add create/edit interactions and wire the existing bioremediation admin module into the library</name>
  <files>components/admin/case-library/case-form-dialog.tsx, app/admin/bioremediation/page.tsx</files>
  <read_first>
    components/record-edit-modal.tsx
    app/admin/bioremediation/cases/actions.ts
    lib/case-library/schema.ts
    lib/case-library/types.ts
    app/admin/bioremediation/page.tsx
    .planning/phases/01-case-library-governance/01-CONTEXT.md
  </read_first>
  <action>Create `components/admin/case-library/case-form-dialog.tsx` as a `'use client'` dialog component that can handle both create and edit modes against `upsertBioremediationCase`, using the exact structured fields from `caseInputSchema` and showing inline error/loading states. Update `app/admin/bioremediation/page.tsx` to add a clear Spanish-language entrypoint to `/admin/bioremediation/cases`, such as a card or CTA that positions the case library as the governed source for future assistant grounding. Keep the interaction lightweight and admin-first: no wizard, no multi-step flow, no separate approval dashboard. Per D-01 and D-02, status changes remain available to any admin via the management UI.</action>
  <acceptance_criteria>
    `rg -n "^'use client'" components/admin/case-library/case-form-dialog.tsx` returns a match.
    `rg -n "upsertBioremediationCase" components/admin/case-library/case-form-dialog.tsx` returns a match.
    `rg -n "issue|zone|species|product_name|treatment_approach|dose|outcome|status" components/admin/case-library/case-form-dialog.tsx` returns all required structured fields.
    `rg -n "/admin/bioremediation/cases" app/admin/bioremediation/page.tsx` returns the new management entrypoint.
    `rg -n "casos|biblioteca|gobern" app/admin/bioremediation/page.tsx components/admin/case-library/case-form-dialog.tsx` returns Spanish-first library copy.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>Admins can reach the case-library workflow from the existing bioremediation admin module and use a product-side dialog to create or edit structured cases without touching the database.</done>
</task>

<task type="auto">
  <name>Task 3: Document the manual smoke checklist required for Phase 1 validation</name>
  <files>.planning/phases/01-case-library-governance/01-MANUAL-CHECKLIST.md</files>
  <read_first>
    .planning/phases/01-case-library-governance/01-VALIDATION.md
    .planning/phases/01-case-library-governance/01-01-PLAN.md
    .planning/phases/01-case-library-governance/01-02-PLAN.md
  </read_first>
  <action>Create `.planning/phases/01-case-library-governance/01-MANUAL-CHECKLIST.md` with explicit numbered steps for `pnpm dev` validation. Cover create, edit, reload persistence, status transitions across `draft` -> `approved` -> `retired`, visibility of `author`, `last_reviewed_at`, and status in the table, confirmation that only `approved` is labeled usable for assistant grounding, and non-admin denial or redirect behavior through `/admin/bioremediation/cases`. Include exact routes, expected badges, and the save/reload expectations needed by `01-VALIDATION.md`.</action>
  <acceptance_criteria>
    `rg -n "pnpm dev|/admin/bioremediation/cases|draft|approved|retired|last_reviewed_at|author" .planning/phases/01-case-library-governance/01-MANUAL-CHECKLIST.md` returns all required verification anchors.
    `rg -n "usable for assistant grounding|usable para assistant grounding|utilizable para assistant grounding|elegible" .planning/phases/01-case-library-governance/01-MANUAL-CHECKLIST.md` returns an explicit approved-only grounding check.
    `rg -n "no admin|non-admin|redirect|/dashboard|/auth/login" .planning/phases/01-case-library-governance/01-MANUAL-CHECKLIST.md` returns the unauthorized-access verification step.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <done>Phase 1 execution ends with a concrete manual verification asset that matches the Nyquist validation expectations already documented for the phase.</done>
</task>

</tasks>

<verification>
Run `pnpm lint && pnpm build`. Then execute the manual checklist in `.planning/phases/01-case-library-governance/01-MANUAL-CHECKLIST.md` during `pnpm dev` to confirm CRUD, review metadata, approval semantics, and admin-only access.
</verification>

<success_criteria>
Aquavet can reach the case library from the existing admin surface, manage cases in Spanish through a server-rendered table plus lightweight dialog interactions, review publication metadata directly in the product, and verify the full Phase 1 flow with an explicit smoke checklist.
</success_criteria>

<output>
After completion, create `.planning/phases/01-case-library-governance/01-02-SUMMARY.md`
</output>
